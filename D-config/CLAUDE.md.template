# 全局规则（所有项目生效）

## 输出质量：交付前自检

每一条输出 — 无论是工具结果、代码修改、分析还是结论 — 在交付前必须自评。标准 90+，目标 100%。

- **交付前问自己**：这条输出值得我押上信誉吗？可读、正确、完整吗？如果用户抽查，会不会有问题？
- **发现有问题（乱码、结果不确定、边界情况遗漏），主动修复。** 不要交付 60-70 分的东西等用户来发现。用户不应该当 QA。
- **遇到限制**（编码问题、工具缺陷、数据模糊），诊断并解决根因，不要打补丁或交付降级结果。
- **对外部工具结果尤其**（API 响应、shell 输出、图片分析）：在呈现给用户之前验证输出是否格式正确、内容完整。

适用于每条回复。不只是用户纠正你的时候。默认标准是 excellence。

## 数据读取规则（强制执行）

核心原则：**你读到的 ≠ 用户看到的。**

### 1. 选对工具：含表格的文档禁止用 Read 工具

| 文件类型 | 必须使用的工具 |
|---------|-------------|
| `.xlsx` / `.xlsm` / `.csv` / `.tsv` | xlsx skill（openpyxl 解析） |
| `.docx` 含表格 | docx skill 或 OfficeCLI |
| `.pptx` | OfficeCLI |
| `.pdf` 含表格 | pdf skill（pdfplumber 表格提取模式） |
| 飞书文档含表格/图表/视频 | lark-doc +fetch --detail full |
| 企业微信文档 | wecomcli-doc / wecomcli-smartsheet |

### 2. 提取后强制输出"数据清单"

读完任何含表格/多维度数据的文件后，分析之前必须输出：
- 文件、Sheet/表数量
- 每表：行×列、维度列（穷举所有取值）、数值列
- 特殊标记：合并单元格、空列等

### 3. 禁止行为
- 禁止看到空白单元格就打 `/`：先确认是真空白还是工具没渲染
- 禁止只汇总前 N 行就下结论
- 禁止用"等"字省略维度取值，必须穷举完整
- 禁止省略空维度

### 4. 交叉校验
- 有合计行必须一致；不一致先怀疑自己的提取逻辑
- 无合计行时取 2-3 个数据点手工验算

## Vision 分析 (Qwen)

**DeepSeek 模型不能处理图片/视频。** 用户提供图片/视频路径时，立即调用：
```bash
python {{USER_HOME}}/qwen-vision-mcp/analyze.py "<file path>" "<user's question>"
```
不要回复"I can't view images/video"——先调脚本。

## 纠正时进入深度模式

当用户纠正、指出遗漏或补充答案时，信号=话题重要。切换深度模式：
- **多角度**：安全、UX、性能、可维护性、产品等角度审视
- **自验证**：交付前挑战自己的答案。假设了什么？漏了什么边界？
- **深度和广度**：不只修复表面的纠正，深挖底层原理，拓展相关影响

**正常模式（用户未纠正）**：保持高效，不过度展开。

## Office 工具选择（速查）

| 文件格式 | 默认工具 |
|---------|---------|
| `.pptx` | OfficeCLI（唯一选择） |
| `.pdf` | pdf skill |
| `.docx` | OfficeCLI 优先，docx skill 备用（模板合并/可视化预览/公式 → OfficeCLI；修订跟踪 → docx skill） |
| `.xlsx/.xlsm` | OfficeCLI 优先（透视表/公式/高级图表 → OfficeCLI；数据清洗/CSV → xlsx skill） |

**规则：功能重叠时按上述选择，不要两个都调，不要中途切换。**

## 数据可视化

- 主动建议图表方案，不等用户要求
- 偏好多样：柱状图、散点气泡、热力图、仪表盘
- 图表要独立讲故事：标注关键点、颜色区分、结论性标题
- 工具链：matplotlib + Microsoft YaHei

## 素材脚本

写素材脚本前，**必须先读取** `{{D_DRIVE}}\claude-config\docs\creative-script-sop.md` 并遵循全部流程。

## AI 生图提示词

用户要求写 AI 生图提示词（中英文）时，**必须先读取** `{{D_DRIVE}}\claude-config\docs\ai-image-prompt-spec.md` 并按其中结构输出。

## 项目管理

本项目下有 3 个子项目，各有独立 CLAUDE.md 和 memory：
- **幻宠帝国**：UA 投放，飞书/企微文档链接在项目 CLAUDE.md 中
- **GTS**：3 留测试仪表盘，Base 在飞书
- **日常**：杂项事务

